import Register from '@structure/pages/register/Register';
import RoutePath from './path';
const registerRoute = [
   {
      path: RoutePath.register,
      element: <Register />,
   },
];

export default registerRoute;
