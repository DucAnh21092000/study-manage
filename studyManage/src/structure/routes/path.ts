const createPlan = '/student/studyPlan/createPlan';
const overView = '/overview';
const listPlan = '/student/listPlan';
const detailPlan = '/student/plan/detail/:id';
const noteList = '/note/list';
const targetList = '/target/list';
const calendar = '/calendar';
const register = '/register/:id';
const currentSchedule = '/schedule';

const RoutePath = {
   createPlan,
   overView,
   listPlan,
   detailPlan,
   noteList,
   targetList,
   calendar,
   register,
   currentSchedule,
};

export default RoutePath;
