import ListTarget from '@structure/pages/target/list-note/ListTarget';
import RoutePath from './path';
const targetRoute = [
   {
      path: RoutePath.targetList,
      element: <ListTarget />,
   },
];

export default targetRoute;
